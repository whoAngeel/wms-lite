meta {
  name: LOGOUT
  type: http
  seq: 4
}

post {
  url: {{URL}}/api/v1/auth/logout
  body: json
  auth: inherit
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
